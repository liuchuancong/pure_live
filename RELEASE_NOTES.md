# Pure Live v2.0.23

本版本基于上游 `v2.0.22`，重点完善小窗播放、弹幕体验和可重复发布构建。

## 新增与优化

- Android 系统画中画、Windows 小窗和应用内悬浮窗支持显示弹幕。
- 小窗弹幕支持独立开关、原始平台颜色或自定义颜色、字号、速度、透明度、显示区域、最大数量、发送间隔、FPS 和自动缩放。
- 小窗使用独立弹幕控制器，降低主播放器与小窗状态相互干扰。
- 应用内悬浮窗切换时保留弹幕与直播连接资源，关闭后正常释放。
- 修复 Windows 构建中的移动端屏幕方向插件目标问题。
- 更新 Flutter、FFmpeg 构建产物地址和 GitHub Actions 版本。

## 下载说明

- Android：按设备架构选择 `arm64-v8a`、`armeabi-v7a` 或 `x86_64` APK。
- Windows：下载 `PureLive-pip-danmaku-windows-x64.zip`，解压后运行。
- `SHA256SUMS.txt` 可用于校验下载文件完整性。

Android APK 使用此仓库专用的持续集成签名。若设备上已有其他签名来源的同包名应用，需要先备份应用数据再安装本版本。
