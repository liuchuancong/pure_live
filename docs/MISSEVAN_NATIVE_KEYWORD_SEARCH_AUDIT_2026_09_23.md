# 猫耳 FM 官网直播关键词搜索接入（2026-09-23）

## 生产合同

官网当前直播页加载的 `bundle.22fb1751.js` 声明匿名 `GET /api/v2/chatroom/search`。本机对官网直接只读请求确认：`s` 是搜索词，`p` 是原生页号，`page_size` 可取 20 或 30；无查询词、`keyword` 或 `q` 返回 HTTP 400。`s=test` 第一页返回 20 条、总量 193/10 页，第二页返回另一批 20 条；`s=音乐` 第一页总量 4187/210 页，包含开播和未开播结果。搜索条目有房间 ID、主播身份、`status.open` 和 `statistics.score`，不含可当作并发人数的字段。仅提取公开元数据；官网 HTML/JS 样本与脱敏探针摘要保存在忽略目录 `local-artifacts/reference/`。

## 源码与验证

猫耳适配器现把非数字词交给官网原生分页搜索，精确房间号/官方链接仍只查详情；未知网页地址不转为关键词。结果保留开播和未开播房间，热度仍来自 `score`，搜索阶段不请求媒体。响应页号、页大小、数量和条目结构严格校验，取消令牌沿现有 HTTP 链传递；UI 能力从“精确房间”更新为“含未开播的原生分页”。两项新增合同回归在旧实现红灯。

Focused 记录 `20260923T044754255Z-quality-focused.json`：猫耳适配器、搜索生命周期和搜索页布局 **116/116**，全库 Analyze 无问题；格式化 5 个 Dart 文件。注册适配器生产关键词探针 **1/1**：前两页各 20 房间，合计 8 开播 / 32 未开播，页间有新增身份，媒体请求 0；忽略目录记录 `missevan-keyword-search-probe-20260923.json`。探针文件定向 Analyze 无诊断。Android/Windows 界面点击、实际播放和录制仍归当前候选集中验收；本批未操作手机或构建安装包。
