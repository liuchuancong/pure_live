# 网页搜索直播链接回流：小红书与 TTing/FLEX（2026-09-23）

内置网页搜索的链接检测此前没有接入这两个已注册适配器：打开小红书直播间或 TTing/FLEX 频道直播页后，应用内房间确认不会出现。新增两条动作合同的红灯，旧解析器返回空。

现在复用各平台现有的严格链接解析器，把小红书 `/livestream/{roomId}` 和 TTing/FLEX `/channels/{channelId}/live` 映射为稳定房间身份。搜索、普通内容、额外路径、仿冒域名仍不映射；小红书短链需要有界异步跳转，不在同步网页检测器中展开；OPENREC 公开频道链接缺少收藏所需的数字 owner ID，继续由其精确查询链确认身份。

三文件定向回归（网页解析、小红书分享链接、TTing 应用）**83/83**，两文件 Analyze 无诊断。原生 WebView 导航和当前候选双端操作仍待集中验收；本批未操作手机或构建安装包。
