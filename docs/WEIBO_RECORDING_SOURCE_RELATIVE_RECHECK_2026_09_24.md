# 微博短录：同输入内容与时钟复核（2026-09-24）

基线业务源码 `8f755028`。本批只修订 [只读生产录制探针](../tool/probes/weibo_recording_probe_test.dart)的验收判据，不修改播放器、录制器或官方接口；没有操作手机。媒体与临时 URL 仅保存在忽略目录 `local-artifacts/weibo-recording-20260924/`。

## 首次失败与同输入诊断

首次现网短录使用当前推荐场次，生产链路完成手动停止、输入排空、两段 TS 合并，最终 MP4 严格完整解码退出码 0 且 stderr 为空；探针仍在 `knownInternalGapCount == 0` 失败。留存同次源 FLV 的视频包已有 53 处已知间隙，最终 MP4 为 26 处；绝对零间隙不属于这次源流的真实合同。对应质量记录 `20260923T161714711Z-quality-focused.json`，失败样本目录 `weibo-1790180188917415/`，原报告未覆盖或删除。

第一版源对成品逐帧比较继续在另一现网样本的音频时钟门槛失败：源对 MP4 偏移范围为 1.126984 ms，超过两音频采样点。该样本保留在 `weibo-1790180416033523/`，质量记录 `20260923T162031104Z-quality-focused.json`。用**同一份留存 FLV**单次直接 remux 为 MP4 后，源对单次参考也产生相同的 1.126984 ms 范围；单次参考对生产两段成品为 0。两轨的源、参考、成品逐帧内容和顺序相同，故这项差异不是两段拼接新增的时钟阶跃。两个失败样本的原始报告、FLV、TS、MP4、packet JSON 与 framemd5 均保留。

## 最终探针门禁与结果

探针现在保留源与成品 packet 时间线，先完整解码，再从同次已提交的 FLV 生成**单次直接 remux 参考**；分别要求源对参考、参考对成品两轨逐帧内容与顺序一致，并要求参考对成品的时钟偏移范围不超过视频 `2/90000` 秒、音频 `2/44100` 秒。它仍检查同场次身份、动态新鲜地址、两段以上、持续写入、手动停止/排空、无尾部丢弃、成功收尾、完整解码和至少 10 秒音视频共同覆盖。单次 remux 仅用于诊断参照，不代替生产录制路径。

当前同批实际网络复验与既有 `FrameHashTimeline` 单测共 **12/12 通过**，记录 `20260923T162239341Z-quality-focused.json`。样本 `weibo-1790180543295538/` 有 2 段 TS，最终 MP4 **2,950,705 B**、SHA-256 `94FA7381AB6970F92E3B2A668DF8FB4A2C27649E1DF79A66A8A77AFCC70EE35E`；516 个完整 FLV 头/标签已提交给本地 relay，捕获未截断。视频 246 帧、音频 266 帧的源→参考→成品有序内容一致；参考→成品两轨时钟偏移范围均为 **0**。源→参考音频范围为 1.013605 ms，独立列示而非抹平。完整 MP4 严格解码通过，stderr 为空；录制停止与收尾释放均完成，结束后没有残留的 ffmpeg 或 flutter_tester 进程。

这只证明该场次、该时段的 Windows 本机 FFmpegKit 短录与同次输入保真；不等于 Windows 客户端 GUI 播放、Android 原生、任意场次/长录或全平台发布验收。
